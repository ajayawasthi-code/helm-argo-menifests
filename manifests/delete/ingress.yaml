apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: delete
  labels:
    app: delete
spec:
  ingressClassName: ingress-appgateway
  rules:
  - host: delete-task.ajay.site
    http:
      paths:
      - pathType: Prefix
        path: "/"
        backend:
          service:
            name: delete
            port: 
              number: 80
